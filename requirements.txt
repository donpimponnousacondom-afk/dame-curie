# Core runtime — everything Maxwell needs to start and hold a conversation.
# Nothing in this file is optional; nothing outside it is required.
discord.py-self>=2.0.0     # Discord transport (self-bot)
aiohttp>=3.14.0            # LLM provider + admin API HTTP
aiofiles>=24.1.0           # async file IO
python-dotenv>=1.0.0       # .env loading
numpy>=2.5.1               # RAG vector math (cosine search)
python-chess>=1.999        # chess_* tools: board state, legal moves, engine
pillow>=10.0.0             # chess board PNG rendering (also used by HD image)
tiktoken==0.14.0
regex==2026.9.3
requests==2.34.2
